- title("Recipes")
.row
  .col-md-9
    %h1
      %i.fa.fa-cutlery
      = "Phil's Curated #{params[:s].blank? ? "" : "#{params[:s]} "}#{@current_tags ? "#{@current_tags}" : "Recipes"}"
  .col-md-3
    - if params[:v] == "grid"
      .btn-group.view-btn-group{"aria-label" => "view button group", :role => "group"}
        = link_to ('<i class="fa fa-list"></i>').html_safe, params.merge({v: "list"}), class: "btn btn-default list-view-btn"
        %button.btn.btn-default.grid-view-btn.disabled{:type => "button"}
          %i.fa.fa-th-large
    - else
      .btn-group.view-btn-group{"aria-label" => "view button group", :role => "group"}
        %button.btn.btn-default.list-view-btn.disabled{:type => "button"}
          %i.fa.fa-list
        = link_to ('<i class="fa fa-th-large"></i>').html_safe, params.merge({v: "grid"}), class: "btn btn-default grid-view-btn"
.row
  .col-md-9
    - if params[:v] == "grid"
      .row
        - @recipes.each do |recipe|
          .col-lg-3.col-sm-4.col-xs-6.grid-img-container
            = link_to image_tag("recipes/#{recipe.image}"), recipe_path(recipe), class: "thumbnail img-responsive"
    - else
      - @recipes.each do |recipe|
        .recipe
          = link_to recipe.name, recipe_path(recipe)
    = paginate @recipes
  .col-md-3
    %ul.list-group
      - @tags.each do |g|
        - g.each do |k,v|
          %li.list-group-item
            %span.badge
              = v
            = link_to k, recipes_path({t: k})
