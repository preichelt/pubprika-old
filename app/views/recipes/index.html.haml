- title("Recipes")
%h1
  %i.fa.fa-cutlery
  Phil's Curated Recipes
%table
  %thead
    %tr
      %th Name
  %tbody
    - @recipes.each do |recipe|
      %tr
        %td= link_to recipe.name, recipe_path(recipe)

= paginate @recipes
